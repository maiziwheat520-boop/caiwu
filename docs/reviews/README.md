# Independent reviews

Review reports are immutable evidence for a specific branch/commit. Name them
`YYYY-MM-DD-<task>-<reviewer>.md`. A later verification appends a new report or a
clearly dated resolution section; it does not erase the original finding.

Required fields for each finding: severity, file/line, violated invariant,
failure scenario, and acceptance condition.
